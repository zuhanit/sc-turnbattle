import skill.skill;
import skill.detail;

var charSkillObject = 0;

function registerSkills() {
    const skill = skill.Skill.alloc();

    const qDetail = detail.Detail.alloc();
    qDetail.name = EPD(Db("마법의 화살"));
    qDetail.mp = 2;
    qDetail.description = EPD(Db("맞으면 30의 피해를 입히는 마법의 화살을 발사합니다."));

    const wDetail = detail.Detail.alloc();
    wDetail.name = EPD(Db("얼음송이"));
    wDetail.mp = 1;
    wDetail.description = EPD(Db("3X3 범위 내의 적을 둔화시키는 영역을 3초간 생성합니다."));

    const eDetail = detail.Detail.alloc();
    eDetail.name = EPD(Db("기절"));
    eDetail.mp = 3;
    eDetail.description = EPD(Db("1초간 정신을 집중한 뒤 지정한 지역 3X3 내의 적에게 \x1130의 피해\x04를 입히며 0.5초간 \x19기절\x04시킵니다."));

    const rDetail = detail.Detail.alloc();
    rDetail.name = EPD(Db("거합베기"));
    rDetail.mp = 2;
    rDetail.description = EPD(Db("\x071턴 뒤 \x04지정한 위치에 유성을 낙하시킵니다. 이 유성 폭발에 맞은 적은 \x1170의 피해\x04를 입습니다"));

    skill.q = qDetail;
    skill.w = wDetail;
    skill.e = eDetail;
    skill.r = rDetail;

    charSkillObject = skill;
}